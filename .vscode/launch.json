{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Запустить Бота (main.py)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py", // ВСЕГДА запускать main.py
            "console": "integratedTerminal",
            "justMyCode": true,
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
